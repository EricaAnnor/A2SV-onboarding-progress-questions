class Solution:
    def findComplement(self, num: int) -> int:
        n_n = bin(num)
        ans = ''
        for x in n_n[2:]:
            if x == '1':
                ans += '0'
            else:
                ans += '1'

        return int(ans,2)
        
