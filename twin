t = int(input())

for _ in range(t):
    n = int(input())
    nums = list(map(int,input().split()))
    check = max(nums)
    res = []

    for x in nums:
        res.append(check-x + 1)
    print(*res)
